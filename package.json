{
  "name": "chat-or-dare",
  "version": "1.0.0",
  "description": "An anonymous chat with random truth-or-dare questions. Made with Express, Redis and React.",
  "main": "ecosystem.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pm2": "env-cmd pm2",
    "build-all": "env-cmd npm run build --workspaces --if-present"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/server",
    "packages/client"
  ],
  "dependencies": {
    "env-cmd": "^10.1.0"
  }
}
